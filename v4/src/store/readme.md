This directory contains package-local Jotai atoms for the resume draft and hydration state.

The store responsibility is intentionally separate from external services, schemas, and UI components because it owns client-side application state coordination.
